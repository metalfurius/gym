name: Build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-android:
    name: Build Android
    uses: ./.github/workflows/build-android.yml

  build-ios:
    name: Build iOS
    uses: ./.github/workflows/build-ios.yml
