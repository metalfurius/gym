name: Create Release

on:
  workflow_run:
    workflows: ["Version Bump"]
    types:
      - completed
    branches: [master]

permissions:
  contents: write
  actions: read

jobs:
  build-android:
    name: Build Android APK
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/build-android.yml

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build-android
    # Only run if the version bump workflow succeeded and android build completed
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Ensure latest master
        run: |
          git pull origin master

      - name: Get current version
        id: version
        run: |
          VERSION=$(node -p "require('./manifest.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Current version: $VERSION"

      - name: Check if tag exists
        id: check-tag
        run: |
          if git rev-parse "v${{ steps.version.outputs.version }}" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Download Android APK artifact
        if: steps.check-tag.outputs.exists == 'false'
        uses: actions/download-artifact@v4
        with:
          name: my-workout-tracker-v${{ steps.version.outputs.version }}-debug-android
          path: ./artifacts

      - name: Rename APK file
        if: steps.check-tag.outputs.exists == 'false'
        run: |
          mv ./artifacts/app-debug.apk ./artifacts/my-workout-tracker-v${{ steps.version.outputs.version }}.apk

      - name: Generate release notes
        if: steps.check-tag.outputs.exists == 'false'
        id: release-notes
        run: |
          # Get the previous tag
          PREVIOUS_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          
          if [ -z "$PREVIOUS_TAG" ]; then
            # No previous tag, get all commits
            COMMITS=$(git log --pretty=format:"- %s (%h)" --no-merges HEAD~20..HEAD 2>/dev/null || git log --pretty=format:"- %s (%h)" --no-merges -20)
          else
            # Get commits since last tag
            COMMITS=$(git log --pretty=format:"- %s (%h)" --no-merges ${PREVIOUS_TAG}..HEAD)
          fi
          
          # Create release notes
          NOTES="## What's Changed
          
          $COMMITS
          
          ## Downloads
          
          - **Android APK**: my-workout-tracker-v${{ steps.version.outputs.version }}.apk
          
          **Full Changelog**: https://github.com/${{ github.repository }}/compare/${PREVIOUS_TAG:-initial}...v${{ steps.version.outputs.version }}"
          
          # Save to file for the release action
          echo "$NOTES" > release_notes.md

      - name: Create Git tag
        if: steps.check-tag.outputs.exists == 'false'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "v${{ steps.version.outputs.version }}" -m "Release v${{ steps.version.outputs.version }}"
          git push origin "v${{ steps.version.outputs.version }}"

      - name: Create GitHub Release
        if: steps.check-tag.outputs.exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: Release v${{ steps.version.outputs.version }}
          body_path: release_notes.md
          draft: false
          prerelease: false
          files: |
            ./artifacts/my-workout-tracker-v${{ steps.version.outputs.version }}.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
